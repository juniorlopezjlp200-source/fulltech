1. Sesión persistente (Postgres)

Usa express-session con store en Postgres (connect-pg-simple o similar).

Configura cookie sid con:

httpOnly: true

secure: true

sameSite: 'lax'

maxAge: 60-90 días

Middleware que cargue req.user desde la sesión.

Crea GET /api/me que devuelva:

{ 
  "authenticated": true, 
  "user": { "id": "...", "name": "...", "email": "...", "avatarUrl": "..." } 
}


Si no hay sesión → { authenticated: false }.

2. UI modo cliente (cuando está logueado)

NavBar condicional:

Mostrar avatar y nombre corto.

Menú con: Tablero, Perfil, Configuración, Referir, Compartir, Soporte, Logout.

Menú flotante móvil con las mismas opciones.

Footer oculto en todas las rutas /mi/*.

En Catálogo, si autenticado, mostrar botón “Ir a mi Tablero”.

3. Rutas del cliente

/mi/tablero: saludo + pedidos recientes + accesos rápidos.

/mi/perfil: editar datos y subir foto.

/mi/perfil/completar: wizard de 3 pasos (Datos → Dirección → Foto).

/mi/configuracion: seguridad, notificaciones, idioma.

/mi/referir: link o código único + compartir.

/mi/soporte: acceso directo a WhatsApp o chat.

4. Perfil del cliente

Campos: nombre, teléfono, email, cédula, dirección completa (provincia, municipio, sector, calle, referencia), fecha nacimiento, género (opcional).

Subida de avatar:

POST /api/profile/avatar (multipart).

Guardar en storage ya configurado (usa secrets).

Devolver avatarUrl.

Actualizar datos:

PUT /api/profile con validaciones.

5. Base de datos (Postgres / Drizzle)

Mantén tablas existentes.

Añade si faltan:

user_profiles: avatar, cédula (encriptada), dirección, etc.

referrals: código único + contador de usos.

sessions: para el store de sesión.

Haz migraciones limpias, sin tocar lo viejo.

6. QA obligatorio

Registro/login crea cookie y reconoce sesión en reingreso.

/api/me devuelve estado real de sesión.

Footer no aparece en /mi/*.

Menú cliente funciona en desktop y móvil.

Subida de avatar al storage actualizada y visible en el header.

Wizard de completar perfil activo en primer login.

7. Reglas generales

Usa DATABASE_URL, SESSION_SECRET y storage desde Secrets (no hardcodear nada).

Validar formatos (teléfono, email, cédula).

Limitar tamaño de imagen a 2–5MB (JPG/PNG/WebP).

Proteger rutas privadas con middleware requireAuth.

👉 Mensaje final para ejecutar en Replit:

“Implementa sesión persistente en Postgres (DATABASE_URL + SESSION_SECRET). Crea GET /api/me. Ajusta UI modo cliente: Nav condicional con avatar, menú flotante móvil, footer oculto en /mi/*, rutas /mi/*. Habilita perfil con subida de avatar al storage configurado (desde Secrets). Añade migraciones para user_profiles y referrals. En Catálogo, si autenticado, muestra ‘Ir a mi Tablero’.”